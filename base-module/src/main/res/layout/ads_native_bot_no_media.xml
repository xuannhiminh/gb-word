<com.google.android.gms.ads.nativead.NativeAdView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_background">

        <!-- AdChoices -->
        <FrameLayout
            android:id="@+id/ad_choices_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_margin="5dp"/>

        <!-- Horizontal Layout: Icon + Texts -->
        <LinearLayout
            android:id="@+id/ll"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/ad_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_ad_badge_leaf"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:text="Ad"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                android:textSize="8sp"
                android:layout_gravity="left"
                android:layout_marginEnd="8dp"
                />

            <!-- App Icon -->
            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/ad_app_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/rounded_background_4"
                android:scaleType="centerCrop"/>

            <!-- Vertical Text Block -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <!-- Headline -->
                <TextView
                    android:id="@+id/ad_headline"
                    android:layout_width="match_parent"
                    android:layout_height="20dp"
                    android:layout_marginBottom="2dp"
                    android:text="StreamYard is a live streaming studio"
                    android:textColor="@color/text1"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:maxLines="1"
                    android:ellipsize="end"/>

                <!-- Body -->
                <TextView
                    android:id="@+id/ad_body"
                    android:layout_width="match_parent"
                    android:layout_height="36dp"
                    android:text="The easiest way to create professional livestream. Get started today."
                    android:textColor="@color/text1"
                    android:textSize="11sp"
                    android:maxLines="3"
                    android:textFontWeight="400"
                    android:ellipsize="end"/>
            </LinearLayout>
        </LinearLayout>

        <!-- CTA Button -->
        <TextView
            android:id="@+id/ad_call_to_action"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_below="@id/ll"
            android:layout_marginTop="16dp"
            android:background="@drawable/button_ads"
            android:gravity="center"
            android:text="Let’s Start"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:paddingVertical="12dp"/>
    </RelativeLayout>
</com.google.android.gms.ads.nativead.NativeAdView>
